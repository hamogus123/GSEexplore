I want to build an R Shiny application that lets users fetch a GEO study by accession (GSE...) and interactively explore the expression data. The app should use GEOquery to retrieve the dataset, parse it into an ExpressionSet, and expose the expression matrix and sample metadata for downstream analysis. Provide QC views (sample distributions and PCA) and a differential expression workflow where users select a phenotype column and choose two groups for comparison. Structure the project as an R package to simplify dependency management and future updates, add a small set of test that tests that cover loading/parsing and the analysis function outputs, and include a vignette that walks through a complete example using GSE63310. Please create a plan for this app and write it to plan.md

